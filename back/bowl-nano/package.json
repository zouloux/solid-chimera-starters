{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "author": "{{author}}",
  "main": "",
  "license": "MIT",
  "scripts": {
    "reset": "rm -rf package-lock.json node_modules && npm i",
    "dev" : "echo 'no dev command'",
    "build" : "echo 'no build command'",
    "create-version": "printf \"$(date +%s)\" > public/app/data/version.txt"
  },
  "devDependencies": {
    "@zouloux/chimera-ci": "^0.18.0"
  }
}
