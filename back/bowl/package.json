{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "author": "{{author}}",
  "main": "",
  "license": "MIT",
  "scripts": {
    "reset": "rm -rf package-lock.json node_modules && npm i"
  },
  "devDependencies": {
    "@zouloux/chimera-ci": "^0.18.0"
  }
}
